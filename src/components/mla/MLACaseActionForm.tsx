"use client";
export default function MLACaseActionForm({ id }: { id: string }) { return <form onSubmit={async e=>{e.preventDefault(); const f=new FormData(e.currentTarget); await fetch(`/api/mla/cases/${id}/action`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))}); location.reload();}} style={{display:'grid',gap:8}}><textarea name="internalNote" placeholder="Add MLA-office monitoring note" required/><input name="assignedDepartment" placeholder="Assign/recommend canonical department"/><select name="status" defaultValue="Under Review"><option>Under Review</option><option>Assigned</option><option>Escalated</option><option>Action Reported</option><option>More Information Requested</option><option>Resolved</option><option>Reopened</option></select><button>Add note / update monitoring</button></form> }
