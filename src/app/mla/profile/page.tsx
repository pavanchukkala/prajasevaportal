import { requireMLASession,getMLAData } from "@/lib/mla"; import { MLAWorkspace,Panel } from "@/components/mla/MLAWorkspace"; export const dynamic="force-dynamic"; export const revalidate=0; export default async function Page(){const u=await requireMLASession(); const d=await getMLAData(); return <MLAWorkspace user={u} current="/mla/profile" buildId={d.system.buildId} commitSha={d.system.commitSha} refreshedAt={d.refreshedAt}><h1>Profile</h1><Panel title="Authenticated MLA workspace profile"><p>User: {u.username}</p><p>Role: {u.role}</p><p>Permissions: view complaints, view AI assessments, view authorized evidence, assign/recommend routing, add MLA monitoring notes, escalate, request review, monitor department responses. Original citizen text, evidence, and historical AI output are read-only.</p></Panel></MLAWorkspace>}
